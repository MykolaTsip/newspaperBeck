module.exports = {
    DB_NAME: process.env.DB_NAME,  // || 'pyak',
    USER_NAME: process.env.DB_USER_NAME, // || 'root',
    USER_PASSWORD: process.env.DB_USER_NAME_PASS, // || 'user',
    DIALECT_DB: process.env.DIALECT_DB, // || 'mysql',
    HOST_DB: process.env.HOST_DB, // || 'localhost'
};
